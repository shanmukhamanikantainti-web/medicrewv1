{
  "name": "medicrew-bridge",
  "version": "1.0.0",
  "description": "MediCrew local bridge server — polls device JSON stream and pushes to Supabase Realtime",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4",
    "dotenv": "^16.4.7",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
